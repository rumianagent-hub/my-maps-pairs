# Firebase environment config for Cloud Functions
# Copy to firebase/.env and fill in real values
# See: https://firebase.google.com/docs/functions/config-env

# These are automatically available via Firebase Admin SDK
# when deployed — no manual configuration needed.

# For local dev, the emulators use FIREBASE_CONFIG env var automatically.
# You typically do NOT need to set anything here for emulator use.

# If you use any external APIs in functions, add them here:
# SOME_EXTERNAL_API_KEY=your_key_here
