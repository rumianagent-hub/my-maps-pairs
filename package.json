{
  "name": "my-maps-pairs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "firebase/functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "emulators": "cd firebase && firebase emulators:start",
    "deploy": "cd firebase && firebase deploy --only firestore,functions"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
